{
  "name": "@swords2silenced/api",
  "version": "0.1.0",
  "private": true,
  "description": "API client and utilities for Swords to Silenced",
  "main": "index.ts",
  "scripts": {
    "build": "echo 'API package'",
    "test": "echo 'No tests yet'",
    "lint": "eslint src --ext .ts 2>/dev/null || true",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
