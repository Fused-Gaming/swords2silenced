{
  "name": "@swords2silenced/mcp-skills",
  "version": "1.0.0",
  "description": "Fused-Gaming MCP Skills integrated with swords2silenced",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "lint": "echo 'TypeScript validation via tsc during build' && npm run build --dry-run 2>&1 | head -1 || true",
    "validate": "npm run lint && npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.3.0"
  },
  "skills": {
    "enabled": true,
    "installed": [
      "pre-deploy-validator",
      "frontend-design",
      "theme-factory",
      "mcp-builder",
      "skill-creator",
      "ascii-mockup",
      "algorithmic-art",
      "canvas-design"
    ]
  }
}
